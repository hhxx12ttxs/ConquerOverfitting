double smallRadius = 0;
double largeRadius = 0;
if(this.radius > circle.radius){
smallRadius = circle.radius;
largeRadius = this.radius;
}
else if(this.radius < circle.radius){
smallRadius = this.radius;

