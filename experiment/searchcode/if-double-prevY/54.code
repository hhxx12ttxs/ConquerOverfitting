import java.awt.geom.Line2D;
import java.util.Random;

public class Ball {
private Pong pong;

private double theta;
Line2D line1 = new Line2D.Float(x, y, prevX, prevY);
Line2D line2;
if (paddle.getPlayerNumber() == 2)
line2 = new Line2D.Float(paddle.x, paddle.y, paddle.x, paddle.y + paddle.height);

