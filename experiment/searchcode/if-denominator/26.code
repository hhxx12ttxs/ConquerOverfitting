public Fraction(int numerator, int denominator)
{
numeratorV = numerator;
denominatorV = denominator;
if (denominatorV <= 0)
int newNumerator = numeratorV * F.denominatorV;
int newDenominator = denominatorV * F.numeratorV;
if (newDenominator == 0)

