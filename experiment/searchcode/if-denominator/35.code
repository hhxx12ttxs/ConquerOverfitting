numerator = a/c;
denominator = denominator/c;
if(numerator<0&amp;&amp;denominator<0) {
numerator = -numerator;
denominator = b/c;
if(numerator<0&amp;&amp;denominator<0) {
numerator = -numerator;
denominator = -denominator;

