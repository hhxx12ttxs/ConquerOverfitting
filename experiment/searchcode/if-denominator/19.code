public Rational(String name, int n, int d) {
if(n == 0 ) numerator = 1;
else numerator = n;
if(d == 0 ) denominator = 1;
else numerator = n;
if(d == 0 ) denominator = 1;
else denominator = d;
simplify(this);

