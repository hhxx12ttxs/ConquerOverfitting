str = g.getStr();
else {
if (g instanceof AbFeuille || g instanceof AbEtoile)
str = g.getStr();
else
str = &quot;(&quot; + g.getStr() + &quot;)&quot;;
if (d instanceof AbFeuille || d instanceof AbEtoile)

