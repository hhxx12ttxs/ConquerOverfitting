if (e.getName().equalsIgnoreCase(portName)) break;
} else if (instPtr.type == COMPLEXCELL)
{
{
{
if (inst.type == COMPLEXCELL)
cellList.add(inst);
// create ni port list
if (type == COMPLEXCELL)
/** type of instance */inttype;
/** leaf cell or SCCell if complex */Objectnp;
/** x size if leaf cell */doublesize;
{
if (inst.type == COMPLEXCELL)
deleteList.add(inst);

