/*
 * org.nrg.nifti.Nifti_Reader
 * XNAT http://www.xnat.org
break;
case NiftiHeader.DT_COMPLEX:
 *
 * Released under the Simplified BSD.
 *
fi.fileType = FileInfo.GRAY32_FLOAT;
if (imp!=null) {
if (complex && imp.getStackSize()==1)
imp = splitComplexImage(imp);
imp = splitComplexImage(imp);
if (imp!=null) {
if (complex && imp.getStackSize()==1)

