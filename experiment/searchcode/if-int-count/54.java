//Enter file contents here

int SherlockSquare(int low, int high) {
int count = 0, count = 0;
for (int i = low, i <= high, i++) {
root = (int) Math.sqrt(i);
if (root * root == i)++count;
}
return count;
}

