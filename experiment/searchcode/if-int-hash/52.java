package Searching.HashFunc;

public class HTable {
private int[] hash_table;
private int MAX_NUM;
hash_table[hashValue(value)] = hashValue(value);
}

public boolean find(int value) {
if (hash_table[hashValue(value)] == hashValue(value))
return true;
else
return false;
}
}

