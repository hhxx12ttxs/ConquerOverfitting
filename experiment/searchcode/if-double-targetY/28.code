double TargetX;
double TargetY;

double DeltaX;
double DeltaY;

double Arccos;
double Angle;
DistanceToTarget = Math.sqrt( Math.pow( TargetX - PosX, 2 ) + Math.pow( TargetY - PosY, 2 ) );
}

public void move()
{
if( ! collision() )

